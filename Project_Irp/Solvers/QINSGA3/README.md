# QINSGA3 — Quantum-Inspired NSGA-III for the many-objective IRP

This project's contribution: a hybrid that encodes each decision variable as
a quantum angle and evolves it with a quantum rotation gate, while using
NSGA-III's own environmental selection and real-coded variation operators
for everything else. Benchmarked against `Solvers/NSGA3/` (plain NSGA-III,
pymoo) on the same Inventory Routing Problem (IRP) instances.

## Contents

- [Concept](#concept)
- [Modules](#modules)
- [Algorithm](#algorithm)
- [Parameters and references](#parameters-and-references)
- [Design history — what changed and why](#design-history--what-changed-and-why)
- [Objectives](#objectives)
- [Usage](#usage)

## Concept

Each decision variable (a delivery quantity or a client priority) is encoded
as an angle θ ∈ [0, π/2] instead of its real value directly. A *measurement*
collapses θ into the real value:

```
x = xl + cos²(θ) × (xu − xl)          [Li & Wang 2007, eq. 3]
```

Instead of moving x directly, the algorithm moves θ toward a *guide angle* —
the best solution found so far in the same region of the objective space —
through a **quantum rotation gate**:

```
Δθ = α(g) × tanh((θ_guide − θ) / (π/8))      [Li et al., ICNC 2008]
```

with the step size α(g) decaying linearly over the run, from `alpha_max` to
`alpha_min`. This rotation gate is the one mechanism with no equivalent in
classic NSGA-III — everything else in the loop (environmental selection,
crossover, mutation) reuses NSGA-III's own machinery directly, so the
rotation gate is what is actually being benchmarked against NSGA-III's
plain real-coded search.

## Modules

| File | Role |
|---|---|
| `chromosome.py` | `QuantumPopulation` — holds the angle matrix θ, `measure()` (θ → x), and `rotate()` (the three rotation-gate variants). Crossover and mutation are deliberately **not** methods here — see [Design history](#design-history--what-changed-and-why). |
| `algorithm.py` | `run_qinsga3()` — the generational loop, plus all its private helpers (normalisation, niching, guide selection, archive, migration). Shares `IRPProblem` with `Solvers/NSGA3/problem.py` — never duplicated. |
| `main.py` | CLI + API entry point, mirroring `Solvers/NSGA3/main.py`'s shape: `run_qinsga3_solver()` / `run_qinsga3_report()` / `render_from_instance()`. Reuses `Solvers/NSGA3/report_builder.py` and `report.py` unchanged, so both solvers produce an identical report schema. |
| `qinsga3_chromosomes.json` | Cached Pareto chromosomes from the last run(s) — gitignored, regenerated by `main.py`; consumed by `sensitivity/compare_qinsga3_vs_nsga3.py` as the QI-NSGA-III side of the comparison. |
| `qinsga3_report.html` | Generated HTML dashboard — gitignored, regenerated by `main.py`. |

## Algorithm

Per generation, `run_qinsga3()`:

1. **Measure** the parent population (θ → X), **evaluate** f1–f4 and constraints, **penalise** infeasible solutions (feasibility-first).
2. **Archive-update** from the parent's own Pareto front (external elitist archive, never regresses).
3. **Normalise** F via an ideal + nadir hyperplane [Deb & Jain 2014, §IV-A] — sharing the same running ideal/nadir as step 6's elitist survival (pymoo's own `ReferenceDirectionSurvival.norm`, monotonic across generations) rather than recomputing it from scratch each generation — **assign** each solution to its nearest reference direction, and **select guides** — the best Pareto member per niche, with the archive filling niches the current front doesn't cover.
4. **Rotate** — the one step that stays in θ-space, since it is the genuinely quantum-inspired mechanism: `Δθ = α(g) × tanh((θ_guide − θ)/(π/8))`.
5. **Vary in X-space** — measure the rotated population, then apply pymoo's own SBX(η) + PM(η), identical operators to `Solvers/NSGA3/`'s. Re-encode the result back to θ (`θ = arccos(√p)`, `p = (x−xl)/(xu−xl)`) so the next generation's rotation step has an angle to work from. Evaluate the offspring; archive-update from its own front.
6. **Elitist survival** — merge parent and offspring populations and keep only the best `pop_size` via pymoo's own `ReferenceDirectionSurvival` (rank + niching): the same environmental-selection step that defines NSGA-III.
7. **Migrate** every `migration_period` generations: inject the archive's best θ per niche into `n_migrate` individuals, to recover niches the current population has lost.

Repeat for `max_gen` generations; the trimmed external archive (by NSGA-II
crowding distance [Deb et al. 2002, §III-B] if larger than `pop_size`) is
the returned Pareto set.

## Parameters and references

| Parameter | Value | Rationale / reference |
|---|---|---|
| `pop_size` | 200 | Fixed search budget, matched to `Solvers/NSGA3` for a fair comparison |
| `max_gen` | 300 | Same reasoning — equal generation budget across both solvers |
| `alpha_max` | 0.10π | Vmax = ±0.1π — Li et al. (ICNC 2008) |
| `alpha_min` | 0.001π | Vmin = ±0.001π — Li et al. (ICNC 2008) |
| `rotation_type` | `"tanh"` | Aggressive saturation, Li et al. (ICNC 2008). Alternatives: `"tanh_soft"` (empirical, gentler saturation) and `"linear"` (Han & Kim 2002, §II-B, standard continuous-domain adaptation) |
| Initial θ | π/4 ± 0.05 | Maximum superposition — Han & Kim (2002), §II-A. The ±0.05 rad perturbation breaks symmetry so the first measurement doesn't collapse every individual to the same point |
| `noise_scale` (measurement) | 0.02 | Diversity noise on measurement, `σ = noise_scale·|sin(2θ)|·(xu−xl)` — Platel et al. (2009), §4.2. Prevents nearby θ values from collapsing to identical integer routes after the IRP decoder rounds to integers |
| SBX `p_cross` | 0.9 | X-space crossover probability |
| SBX `eta_cross` | 20 | X-space distribution index — matches `Solvers/NSGA3`'s own SBX exactly (Deb, 2001) |
| PM `eta_mut` | 20 | X-space distribution index — matches `Solvers/NSGA3`'s own PM exactly (Deb & Agrawal, 1995) |
| PM `p_mut` | `1 / D` (D = number of decision variables) | Matches NSGA-III's `pm = 1/D` convention (Deb & Jain, 2014) |
| `N_PARTITIONS` (Das-Dennis) | 8 → 165 reference directions for 4 objectives | Same construction as `Solvers/NSGA3` — Das & Dennis (1998), as prescribed by Deb & Jain (2014) |
| `migration_period` | 10 generations | Frequency of archive → population injection (Han & Kim, 2002, migration principle) |
| `n_migrate` | 10 individuals | Number of individuals refreshed from the archive per migration event |
| External archive cap | 500 | Bounded via NSGA-II crowding-distance trimming (Deb et al., 2002, §III-B) when exceeded |

Dominance checks throughout (`_archive_update`) follow Zitzler's (1999,
Def. 2) definition.

## Design history — what changed and why

Two mechanisms were added on top of the algorithm's original design, both
validated with a 20-vs-20 seeded comparison against `Solvers/NSGA3/` on the
IRP's 100-client instance, using a shared ideal/nadir and a Mann-Whitney U
test (see git history for the full before/after numbers):

- **Elitist survivor selection** (step 6 above). Previously, the whole
  population was rotated, crossed and mutated unconditionally every
  generation — nothing ever compared "before variation" to "after" to
  discard individuals that got worse; only the external archive provided
  any elitism. Adding the parent/offspring merge-and-select step
  (`ReferenceDirectionSurvival`, the same one NSGA-III itself uses) roughly
  doubled Hypervolume.
- **X-space variation** (step 5 above). The measurement map
  `x = xl + cos²(θ)(xu−xl)` has `dx/dθ = −(xu−xl)sin(2θ)`, which vanishes at
  θ=0/π/2 and peaks at θ=π/4 — a highly non-uniform mapping. SBX/PM applied
  directly in θ-space meant a fixed-width move in θ produced wildly
  different moves in the real decision variables depending on where θ
  currently sat. Moving variation into X-space (keeping the rotation gate —
  the distinctive quantum mechanism — in θ-space) roughly tripled
  Hypervolume on top of the elitist-selection change.

- **Stateful ideal/nadir normalisation** (step 3 above). Guide selection used
  to call `_normalise_F(F)` fresh every generation, computing ideal/nadir
  from only that generation's population — while the elitist survival step
  (step 6) already used pymoo's own `ReferenceDirectionSurvival.norm`, whose
  ideal/nadir is a running estimate that only ever improves across the whole
  run. Two different, inconsistent normalisations of the same concept were
  in play within the same generation: one driving which guide an individual
  rotates toward, the other driving who survives. Sharing `survival.norm`'s
  values for guide selection too (falling back to the from-scratch estimate
  only on generation 0, before `survival.do()` has run once) fixed this.
  Validated on the IRP's 100-client instance (20 seeds, shared ideal/nadir,
  Mann-Whitney U vs. the pre-fix behaviour): GD and IGD improved
  significantly (p=0.022 and p=0.029), HV improved but just missed
  significance (p=0.060) — see `Solvers/IRP_results_summary.md` for the full
  numbers. Also confirmed on the DTLZ1-7/MaF1-7 synthetic benchmark suite
  (30 runs, M=3/M=4) — see
  `Validation/Benchmarking/dtlz/results/qinsga3/DTLZ_results_summary_qinsga3.md`
  and the MaF equivalent.

A real, smaller quality gap versus `Solvers/NSGA3/` remains on the
100-client instance after all three changes. Two further re-tuning attempts were
tested and **not adopted** because they failed to improve on that gap once
validated against the same NSGA-III reference (rather than only against
other QI-NSGA-III configurations):

- A smaller `alpha_max` (0.01π) and the `"tanh_soft"` rotation variant each
  looked better in isolated internal comparisons (QI-NSGA-III configs
  measured only against each other), but the combination did not improve
  — and slightly worsened — GD/IGD once cross-validated against NSGA-III
  with a shared ideal/nadir. Production keeps `alpha_max=0.10π`,
  `rotation_type="tanh"`.
- An **elitist per-niche guide** (comparing the archive's niche
  representative against the current front's, rather than only filling
  empty niches) and a **diversity-preserving operator**
  [Tayarani-N & Akbarzadeh-T, 2014, *Evolutionary Intelligence* 7:219-239,
  §5] to counteract the premature convergence the guide change caused on
  deceptive multimodal DTLZ problems — both explored on the DTLZ/MaF
  benchmark suite, neither adopted. See `docs/superpowers/specs/` and
  `Validation/Benchmarking/dtlz/results/qinsga3/DTLZ_results_summary_qinsga3.md`
  for the full ablation record.

## Objectives

Identical to `Solvers/NSGA3/` — see
[`Solvers/NSGA3/README.md`](../NSGA3/README.md#objectives). Both solvers
share the same `IRPProblem` (`Solvers/NSGA3/problem.py`), so f1–f4 are
computed by exactly the same code for both algorithms.

## Usage

```bash
python -m Solvers.QINSGA3.main                                       # instance_25_clients.json, 1 run
python -m Solvers.QINSGA3.main --instance 100 --runs 20               # 100-client instance, 20 seeded runs
python -m Solvers.QINSGA3.main --instance 25 --pop 200 --gen 200 --runs 3
```

Or via the local web UI: `python main.py` → select an instance → run
QI-NSGA-III.

To compare against NSGA-III directly (requires both algorithms' caches to
exist for the same instance):

```bash
python -m sensitivity.compare_qinsga3_vs_nsga3 --instance 100
```
